apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui-dev
  labels:
    app: guestbook-ui-dev
spec:
  type: ClusterIP
  selector:
    app: guestbook-ui-dev
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP

