apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-ui-stage
  labels:
    app: guestbook-ui-stage
spec:
  replicas: 2
  selector:
    matchLabels:
      app: guestbook-ui-stage
  template:
    metadata:
      labels:
        app: guestbook-ui-stage
    spec:
      containers:
      - name: guestbook-ui
        image: gcr.io/google-samples/gb-frontend:v5
        ports:
        - containerPort: 80

