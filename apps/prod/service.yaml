apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui-prod
  labels:
    app: guestbook-ui-prod
spec:
  type: ClusterIP
  selector:
    app: guestbook-ui-prod
  ports:
    - port: 80
      targetPort: 80

